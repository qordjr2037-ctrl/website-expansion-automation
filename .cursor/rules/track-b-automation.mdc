---
description: Track B gangara — 백링크 수집·배포·SERP 실험
alwaysApply: true
---

# 웹사이트 확장 자동화 (Track B)

repo: **website-expansion-automation** only. **board-post-automation 수정 금지.**

## 매 Run

1. `core/AGENT_INSTRUCTION_PROMPT.txt` 준수
2. `bash tools/start_gangara_supervisor.sh` (죽었으면 재시작)
3. `core/backlink_verified_store.json` · `backlink_deploy_status.json` 확인
4. `python3 core/run_backlink_auto.py --batch 50` + `run_learning_loop.py --max-cycles 1`
5. 실패 1개 → push · Run 보고 (AGENT_INSTRUCTION_PROMPT 하단)

## 금지

Track A · 비밀 commit · supervisor 중단 방치
